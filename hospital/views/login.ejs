<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>

    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #9b59b6 0%, #6dd5ed 100%); /* Updated gradient to lighter shades */
            margin: 0;
            padding: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
        }
        
        h1 {
            color: #444;
            text-align: center;
            font-size: 2em;
            margin-bottom: 20px;
        }
        
        form {
            max-width: 400px;
            width: 100%;
            padding: 40px;
            background-color: #ffffff;
            border-radius: 12px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
        }
        
        div {
            margin-bottom: 20px;
        }
        
        label {
            display: block;
            margin-bottom: 8px;
            color: #333;
            font-weight: 600;
        }
        
        input[type="text"],
        input[type="password"] {
            width: 100%;
            padding: 12px;
            border: 1px solid #ddd;
            border-radius: 6px;
            font-size: 16px;
            transition: all 0.3s ease;
            background-color: #f9f9f9;
        }
        
        input[type="text"]:focus,
        input[type="password"]:focus {
            border-color: #9b59b6; /* Adjusted to match the theme */
            background-color: #fff;
            box-shadow: 0 0 8px rgba(155, 89, 182, 0.2); /* Adjusted to match the theme */
            outline: none;
        }

        button {
            display: block;
            width: 100%;
            padding: 12px;
            background: linear-gradient(135deg, #9b59b6, #6dd5ed); /* Updated gradient to match background */
            color: white;
            border: none;
            border-radius: 6px;
            font-size: 16px;
            font-weight: bold;
            cursor: pointer;
            transition: all 0.3s ease;
            margin-top: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        
        button:hover {
            background: linear-gradient(135deg, #6dd5ed, #9b59b6); /* Reversed gradient for hover effect */
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.15);
        }

        h5 {
            text-align: center;
            margin-top: 20px;
            color: #444;
            font-size: 15px;
            line-height: 1.6;
        }

        h5 a {
            color: #9b59b6; /* Adjusted to match the theme */
            text-decoration: none;
            font-weight: bold;
            transition: color 0.3s ease;
        }

        h5 a:hover {
            color: #6dd5ed; /* Adjusted hover color to match the theme */
        }
    </style> 
</head>
<body>
    <div>
        <form action="/login" method="post">
            <h1>User Login</h1>
            <div>
                <label for="name">Name:</label>
                <input type="text" id="name" name="name" required>
            </div>
            <div>
                <label for="password">Password:</label>
                <input type="password" id="password" name="password" required>
            </div>
            <div>
                <button type="submit">Login</button>
            </div>
        </form>
        <h5>Not registered yet? <a href="http://localhost:8000/register">Register here</a></h5>
    </div>
</body>
</html>